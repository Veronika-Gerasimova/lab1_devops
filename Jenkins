pipeline {
    agent { 'docker' }
    stages {
        stage('python test') {
             agent { docker } {
          label 'docker'
          image 'python:3.12-alpine'
        }
            steps {
                sh 'python --version'
                    }
                }
            }
        }
